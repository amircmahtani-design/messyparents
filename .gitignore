# Local working space for scripts/social-render.js. Rendered Instagram slides
# are build output, not source — they are regenerated from the guides whenever
# they are needed, and they must not be committed or deployed.
social-preview/

# Node and editor noise.
node_modules/
.DS_Store
