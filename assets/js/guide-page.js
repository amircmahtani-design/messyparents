/* Superseded by guide.js — kept as a marker, not as code.

   This file held an older copy of the guide page behaviour, including its own
   version of the scale-to-fit that was removed from guide.js in August 2026
   for rendering guides at different sizes depending on the reader's window
   height. Nothing loads this file: no HTML references it, the build does not
   emit it, and guide.html loads /assets/js/guide.js instead.

   It is emptied rather than deleted so the path stays valid if anything still
   points at it, and so the fit cannot be reintroduced by someone finding the
   old implementation here and wiring it back up.

   The live behaviour is in assets/js/guide.js. */
